<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<%= form_for @task, url: task_path(@task), method: :put do |f| %>
  <%= f.check_box :completed %>
  <%= f.submit %>
<% end %>
<%= link_to 'Back', tasks_path %>